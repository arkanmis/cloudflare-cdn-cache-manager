# Server Configuration
NODE_ENV=production
PORT=8080
HOST=0.0.0.0

# Cloudflare API Configuration
# Get these values from your Cloudflare dashboard

# Zone ID: Found in Cloudflare Dashboard > Select your domain > Overview (right sidebar)
CF_ZONE_ID=your_cloudflare_zone_id_here

# API Token: Create at Cloudflare Dashboard > My Profile > API Tokens
# Required permission: Zone.Cache Purge
CF_API_TOKEN=your_cloudflare_api_token_here

# Purge Token: (Optional) Secret token for securing the /.purge endpoint
# Set this to any random secure string to require authentication for cache purge requests
# If not set, the /.purge endpoint will work without token authentication
CF_PURGE_TOKEN=your_secret_purge_token_here

# CORS Configuration
# Comma-separated list of allowed domains (without protocol)
# Supports root domain and all subdomains (e.g., mydomain.com matches *.mydomain.com)
ALLOWED_ORIGINS=mydomain.com,mydomain.co.id,alternatedomain.id,alternatedomain.ai
